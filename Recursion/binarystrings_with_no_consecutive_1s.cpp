#include <bits/stdc++.h>
using namespace std;
#define int long long
#define float long double
#define speedup ios_base::sync_with_stdio(0);cin.tie(NULL);cout.tie(NULL)

void Print( int pointer , vector<string> ans, string s ,int N ){
    if( pointer == N ){
        ans.emplace_back( s );
        return;
    }
}

vector<string> generateString(int N) {
    
}

int32_t main() {
    speedup;
    int t = 1;
    cin >> t;
    while( t-- ){
       int n ;cin >> n;
        vector<int> arr(n);
        for( auto& it : arr)
            cin >> it;
 
   }
}